export declare const generateProviderEndpoints: (chainId: string, actionEndpoints: string[]) => {
    chainId: string;
    port: number;
    protocol: string;
    host: string;
    httpEndpoint: string;
    blockchain: string;
}[];
